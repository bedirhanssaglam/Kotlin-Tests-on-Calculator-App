package com.example.calculator.constants

/**
 * Constants used in calculator operations.
 */
class CalculatorConstants {
    companion object {
        // Default value for negative inputs
        const val DEFAULT_NEGATIVE_VALUE: Double = -1.0

        // Default value for the result when no calculation is performed
        const val DEFAULT_RESULT_VALUE: Double = 0.0
    }
}